TEMPLATES_AUTO_RELOAD = False
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_SECURE = True
USER_DEFAULTS = {"user_locale": "en-US", "user_timezone": "UTC" }
THEME = "default"
HEALTHZ = {
  "live": "webapp.checks.liveness",
  "ready": "webapp.checks.readiness"
}
